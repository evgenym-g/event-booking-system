# Event Booking System API

REST API для системы бронирования событий, построенная на FastAPI.

## Возможности

- 🔐 Аутентификация через JWT токены
- 👥 Управление пользователями (регистрация, авторизация, профиль)
- 📅 Управление событиями (создание, редактирование, удаление)
- 🎫 Бронирование событий
- ⭐ Система отзывов и рейтингов
- 🏷️ Категории событий
- 🔒 Защита API через подписи запросов (опционально)

## Требования

- Python 3.11 или выше
- pip (менеджер пакетов Python)

## Установка

### 1. Клонирование репозитория

```bash
git clone <url-репозитория>
cd BPO
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
SECRET_KEY=your_secret_key_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

**Важно:** Замените `your_secret_key_here` на безопасный случайный ключ (минимум 32 символа).

### 5. Инициализация базы данных

База данных SQLite создастся автоматически при первом запуске приложения.

## Запуск

```bash
uvicorn main:app --reload
```

Сервер будет доступен по адресу: `http://127.0.0.1:8000`

## Документация API

После запуска сервера доступна интерактивная документация:

- **Swagger UI:** http://127.0.0.1:8000/docs
- **ReDoc:** http://127.0.0.1:8000/redoc

## Использование API

### 1. Регистрация пользователя

```bash
POST /auth/register
Content-Type: application/json

{
  "username": "user123",
  "password": "password123",
}
```

### 2. Авторизация

```bash
POST /auth/login
Content-Type: application/x-www-form-urlencoded

username=user123&password=password123
```

Ответ содержит `access_token`, который нужно использовать для авторизованных запросов.

### 3. Использование токена

Добавьте заголовок в запросы:

```
Authorization: Bearer <ваш_access_token>
```

### Тестирование через Swagger UI

1. Откройте http://127.0.0.1:8000/docs
2. Выполните запрос `/auth/login` с вашими учетными данными
3. Скопируйте `access_token` из ответа
4. Нажмите кнопку **"Authorize"** вверху страницы
5. Введите: `Bearer <ваш_access_token>`
6. Теперь можно тестировать все защищенные эндпоинты

## Структура проекта

```
BPO/
├── main.py              # Основное FastAPI приложение и эндпоинты
├── auth.py              # Аутентификация и JWT токены
├── models.py             # SQLAlchemy модели
├── schemas.py            # Pydantic схемы
├── crud.py               # CRUD операции с БД
├── database.py           # Настройка подключения к БД
├── dependencies.py      # Зависимости FastAPI
├── signature.py          # Верификация подписей запросов
├── requirements.txt      # Зависимости проекта
├── .env                  # Переменные окружения (создать вручную)
└── README.md             # Этот файл
```

## Основные эндпоинты

### Аутентификация
- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Авторизация и получение токена
- `POST /auth/logout` - Выход из системы

### Пользователи
- `GET /users/me` - Получить данные текущего пользователя
- `GET /users` - Список пользователей (только для админов)
- `GET /users/{user_id}` - Получить пользователя по ID
- `PATCH /users/{user_id}` - Обновить данные пользователя
- `DELETE /users/{user_id}` - Удалить пользователя

### События
- `POST /events` - Создать событие
- `GET /events` - Список событий
- `GET /events/{event_id}` - Получить событие по ID
- `PATCH /events/{event_id}` - Обновить событие
- `DELETE /events/{event_id}` - Удалить событие
- `GET /events/search?q=query` - Поиск событий

### Бронирования
- `POST /bookings` - Создать бронирование
- `GET /bookings/me` - Мои бронирования
- `GET /bookings/{booking_id}` - Получить бронирование
- `PATCH /bookings/{booking_id}` - Обновить бронирование
- `DELETE /bookings/{booking_id}` - Отменить бронирование

### Отзывы
- `POST /reviews` - Добавить отзыв
- `GET /reviews/event/{event_id}` - Отзывы на событие
- `PATCH /reviews/{review_id}` - Обновить отзыв
- `DELETE /reviews/{review_id}` - Удалить отзыв

### Категории
- `POST /categories` - Создать категорию (только для админов)
- `GET /categories` - Список категорий
- `DELETE /categories/{category_id}` - Удалить категорию (только для админов)

## Безопасность

- JWT токены с настраиваемым временем жизни
- Хэширование паролей через bcrypt
- Опциональная верификация подписей запросов (HMAC-SHA256)
- Защита от replay-атак через nonce
- Проверка временных меток запросов

## Разработка

Для разработки рекомендуется использовать виртуальное окружение и следовать PEP 8 стандартам кодирования.
